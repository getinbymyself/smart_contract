// src/services/authService.js
const authService = {
    login: async (credentials) => {
      // 模拟登录，返回成功状态
      return true;
    },
    register: async (userInfo) => {
      // 模拟注册，返回成功状态
      return true;
    },
  };
  
  export default authService;
  